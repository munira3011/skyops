# Upstream provider keys used by config.yaml's model_list.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Clients (e.g. backend/app/gateway/client.py) authenticate to this proxy with this key.
LITELLM_MASTER_KEY=
